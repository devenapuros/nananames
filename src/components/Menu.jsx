import styles from "@/styles/menu.module.css";

export const Menu = () => {
    return (
        <ul className={styles.menu}>
            <li>About</li>
            <li>Github</li>
        </ul>
    );
};
